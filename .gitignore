# 忽略构建产物与本地开发资源
# /target 锚定根目录，盖不住 src-tauri/target，因此用任意深度的 target/
/target
src-tauri/target
frontend/dist
frontend/node_modules

# 本地开发用模型与测试图（不进仓库）
/.dev

# Tauri 生成物：gen/android 需提交（含签名配置与清单自定义，官方推荐），
# 工程内部 .gitignore 已排除 build/ 与本机路径文件
# Tauri generated: gen/android stays committed (official recommendation; its
# inner .gitignore excludes build outputs and machine-local files)

# 系统与编辑器
.DS_Store
Thumbs.db
*.log

# 本地产物（测试用 APK 拷贝）
/*.apk
